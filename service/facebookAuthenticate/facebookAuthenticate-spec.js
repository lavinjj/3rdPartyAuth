describe('facebookAuthenticate', function() {

  beforeEach(module('3rdPartyAuth'));

  it('should ...', inject(function(facebookAuthenticate) {

	//expect(facebookAuthenticate.doSomething()).toEqual('something');

  }));

});
